@{
    ViewData["Title"] = "Cidades";
}

<h1>Cidades Disponíveis</h1>

<h4>Escolha da Cidade para o serviço: @ViewBag.Service</h4>
<hr />

<div class="row">
    <div class="col-md-4">
        <form asp-action="Equips">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="font-weight-bold">Cidades</label>
                <fieldset data-role="controlgroup">
                    @foreach (var city in @ViewBag.Cities)
                    {
                        <input type="radio" name="city" id="@city" value="@city" checked>
                        <label for="@city">@city</label>
                        <br />
                    }
                </fieldset>
            </div>
            <br />
            <div class="form-group" id="btnSend">
                <input type="submit" id="btn" value="Selecionar" class="btn btn-info" />
            </div>
        </form>
    </div>
</div>
