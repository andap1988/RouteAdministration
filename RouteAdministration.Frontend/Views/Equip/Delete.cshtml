@model Models.Equip

@{
    ViewData["Title"] = "Remover Equipe";
}

<h1>Remover</h1>

<h3>Você realmente deseja remover essa equipe?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.City)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.City)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.People)
        </dt>
        <dd class="col-sm-10">
            <select style="width: 250px" multiple>
                @for (var i = 0; i < Model.People.Count; i++)
                {
                    <option value="@Model.People[i].Name">@Model.People[i].Name</option>
                }
            </select>
        </dd>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Remover" class="btn btn-danger" />
    </form>
    <br />
    <br />
    <a asp-action="Index">Voltar para a listagem</a>
</div>